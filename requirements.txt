# Root requirements.txt — Vercel serverless deployment
# sentence-transformers/PyTorch removed (800MB+) → use Gemini Embedding API instead
# pytesseract removed (needs Tesseract binary, not available on Vercel)
# Pillow removed (only needed for OCR path)
# numpy removed (only needed by sentence-transformers)
# gunicorn removed (Vercel uses its own WSGI runner)

flask==3.0.3
flask-cors==4.0.1
PyMySQL==1.1.1
cryptography==42.0.8
bcrypt==4.1.3
PyMuPDF==1.24.5
pinecone-client==4.1.0
openai==1.35.7
python-dotenv==1.0.1
google-generativeai>=0.8.0
